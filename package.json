{
  "name": "bucchinote",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "wrangler pages dev",
    "deploy": "pnpm run build && wrangler pages deploy",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "pnpm exec wrangler d1 migrations apply bn --local"
  },
  "dependencies": {
    "@auth/core": "^0.34.3",
    "@hono/auth-js": "^1.1.0",
    "dotenv": "^17.2.3",
    "hono": "^4.10.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251014.0",
    "@hono/vite-build": "^1.7.0",
    "@hono/vite-dev-server": "^0.18.3",
    "@types/node": "^24.10.0",
    "drizzle-kit": "^0.31.6",
    "drizzle-orm": "^0.44.7",
    "tsx": "^4.20.6",
    "vite": "^6.4.1",
    "wrangler": "^4.45.3"
  }
}